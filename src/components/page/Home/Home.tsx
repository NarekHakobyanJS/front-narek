import { Archive } from '../../organism/Archive/Archive';
import { Header } from '../../organism/Header/Header';

export const Home = () => {
  return (
    <div>
        <Header />
        <Archive />
    </div>
  )
}
